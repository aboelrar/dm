<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="www.dm.com">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main_Screen.Scenario_messages.controller.Scenario_chat.chat"></activity>
        <activity android:name=".Scenario_signup.signup" />
        <activity android:name=".Scenario_login.login"></activity>
        <activity android:name=".Lets_go.lets_go">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main_Screen.MainActivity" />
    </application>

</manifest>